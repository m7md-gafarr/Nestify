import 'package:flutter/material.dart';

class NewReviewsScreen extends StatelessWidget {
  const NewReviewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
